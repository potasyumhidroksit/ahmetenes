# Public origin (behind the TLS proxy this must be the browser-facing HTTPS URL)
EMDASH_SITE_URL=https://ahmetenes.com
SITE_URL=https://ahmetenes.com

# SQLite database file and uploaded media directory (inside the container)
DATABASE_URL=file:./data/data.db
MEDIA_DIR=./data/uploads
